name: Adds all issues project board

on:
  issues:
    types:
      - opened

jobs:
  add-to-project:
    name: Add issue to project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/add-to-project@RELEASE_VERSION
        with:
          project-url: https://github.com/users/awilsoncs/projects/2
          github-token: ${{ secrets.GITHUB_TOKEN }}
